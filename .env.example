# Environment
NODE_ENV=development

# Database (PostgreSQL for production)
# DATABASE_URL=postgresql://user:pass@localhost:5432/pm_suite

# =============================================================================
# AI Provider Mode
# =============================================================================
# "standalone" = Direct LLM calls (default, self-contained)
# "brain" = Delegates to dooz-brain (richer context, organizational memory)
AI_PROVIDER_MODE=standalone

# Brain Provider Config (only used if AI_PROVIDER_MODE=brain)
# BRAIN_URL=http://localhost:3333
# BRAIN_API_KEY=your-api-key

# =============================================================================
# Standalone Mode Config (direct LLM)
# =============================================================================
# AI Router - OpenRouter (Cloud)
# OPENROUTER_API_KEY=sk-or-v1-...

# AI Router - Ollama (Local/Self-hosted)
# OLLAMA_ENABLED=true
# OLLAMA_BASE_URL=http://localhost:11434

# AI Router - Debug logging
# AI_ROUTER_LOGGING=true

# Server
PORT=3000

# =============================================================================
# Bridge Integration
# =============================================================================
BRIDGE_URL=http://localhost:3001

